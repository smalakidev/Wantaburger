<!--Div for images of the burger-->
<div class="container">
   <img src="/assets/images/burger5.png" class="burgerpix">
    <h1 id="title">EAT-DA-BURGER!</h1>
</div>
<div class="container">
    <div class="row">        
        <div class="col-lg-4"><ol>
            {{#each burger}}
            {{#if devoured}}
            <div class='row'>
                <div class='col-lg-12'>
                    <li class='list'>
                        <p class='wait'>{{this.id}}. {{this.burger_name}}</p>
                        <button class='check' data-id="{{this.id}}">Devour it!</button>
                        </li>
                        </div>
                        </div>
            {{/if}}
            {{/each}}
        </ol>
        </div>
        <div class="col-lg-4">         